import React from "react";
import { Container } from '@material-ui/core';

export default class FirstNode extends React.Component {
	render() {
		return (
			<Container maxWidth="sm">
				Writing a file Hello.txt
			</Container>
		);
	}
}